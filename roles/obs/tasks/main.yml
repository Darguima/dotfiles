- name: Installing OBS
  pacman:
    name: obs-studio
    state: latest
  become: true


- name: Copying OBS scenes folder
  copy:
    src: ./scenes
    dest: "{{ linux_user_home }}/.config/obs-studio/basic/"
