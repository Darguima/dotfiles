- name: Installing OBS
  pacman:
    name: obs-studio
    state: latest
  become: true


- name: Copying OBS scenes folder
  copy:
    src: ./scenes
    dest: "{{ ansible_facts['user_dir'] }}/.config/obs-studio/basic/"
