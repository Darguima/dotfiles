- name: Updated & Upgraded pacman
  community.general.pacman:
    update_cache: yes
    upgrade: yes
  become: true

- name: Installed ansible's packages dependencies
  community.general.pacman:
    name:
      - python-setuptools
      - python-lxml
    state: latest
  become: true
