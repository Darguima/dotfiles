- name: Updating & Upgrading pacman
  pacman:
    update_cache: yes
    upgrade: yes
  become: true

- name: Installing ansible's packages dependencies
  pacman:
    name:
      - python-setuptools
      - python-lxml
    state: latest
  become: true
